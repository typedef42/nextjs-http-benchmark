FROM buildkite/puppeteer:latest

# Copy package json files for services
RUN mkdir /app
WORKDIR /app

COPY ./benchmarks/ .
COPY ./ecosystem.config.js .

ENV NODE_ENV=production
RUN npm install --production && npm prune --production

# CMD pm2-runtime start ecosystem.config.js --only benchmark
